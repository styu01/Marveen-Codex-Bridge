diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/__tests__/codex-agent-identity.test.ts marveen-1.21.1-0.1.7/src/__tests__/codex-agent-identity.test.ts
--- marveen-1.21.1-current/src/__tests__/codex-agent-identity.test.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/__tests__/codex-agent-identity.test.ts	2026-07-28 15:40:20.306548195 +0900
@@ -0,0 +1,55 @@
+import { describe, expect, it, vi } from 'vitest'
+import {
+  buildCodexAgentIdentityFiles,
+  generateAgentIdentityFiles,
+} from '../web/agent-scaffold.js'
+
+describe('Codex agent identity generation', () => {
+  it('builds deterministic provider-neutral identity and soul files', () => {
+    const result = buildCodexAgentIdentityFiles(
+      'codex-kutato',
+      'Piackutatást végez és ellenőrzött összefoglalót készít.',
+    )
+
+    expect(result.claudeMd).toContain('# codex-kutato')
+    expect(result.claudeMd).toContain('Piackutatást végez')
+    expect(result.claudeMd).toContain('bela_agent_message_send')
+    expect(result.claudeMd).not.toContain('tmux')
+    expect(result.soulMd).toContain('codex-kutato személyisége')
+  })
+
+  it('does not call either Claude generator for a Codex provider', async () => {
+    const claudeMd = vi.fn(async () => { throw new Error('Claude must not run') })
+    const soulMd = vi.fn(async () => { throw new Error('Claude must not run') })
+
+    const result = await generateAgentIdentityFiles(
+      'codex',
+      'codex-kutato',
+      'Kutatási feladatokat végez.',
+      'gpt-5.6-terra',
+      { claudeMd, soulMd },
+    )
+
+    expect(result.claudeMd).toContain('Kutatási feladatokat végez.')
+    expect(claudeMd).not.toHaveBeenCalled()
+    expect(soulMd).not.toHaveBeenCalled()
+  })
+
+  it('keeps the existing generators for Claude agents', async () => {
+    const claudeMd = vi.fn(async () => 'generated CLAUDE.md')
+    const soulMd = vi.fn(async () => 'generated SOUL.md')
+
+    await expect(generateAgentIdentityFiles(
+      'claude',
+      'claude-kutato',
+      'Kutat.',
+      'claude-opus',
+      { claudeMd, soulMd },
+    )).resolves.toEqual({
+      claudeMd: 'generated CLAUDE.md',
+      soulMd: 'generated SOUL.md',
+    })
+    expect(claudeMd).toHaveBeenCalledOnce()
+    expect(soulMd).toHaveBeenCalledOnce()
+  })
+})
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/__tests__/codex-message-router.test.ts marveen-1.21.1-0.1.7/src/__tests__/codex-message-router.test.ts
--- marveen-1.21.1-current/src/__tests__/codex-message-router.test.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/__tests__/codex-message-router.test.ts	2026-07-28 15:40:20.306548195 +0900
@@ -0,0 +1,79 @@
+import { beforeEach, describe, expect, it, vi } from 'vitest'
+
+const mockDispatch = vi.fn()
+const mockMarkDelivered = vi.fn()
+const mockGetPending = vi.fn()
+
+vi.mock('../logger.js', () => ({
+  logger: { info: vi.fn(), warn: vi.fn(), debug: vi.fn(), error: vi.fn() },
+}))
+vi.mock('../config.js', () => ({ MAIN_AGENT_ID: 'orin' }))
+vi.mock('../db.js', () => ({
+  createAgentMessage: vi.fn(),
+  getPendingMessages: () => mockGetPending(),
+  markMessageDelivered: (...args: unknown[]) => mockMarkDelivered(...args),
+  markMessageFailed: vi.fn(),
+}))
+vi.mock('../web/voice-directive.js', () => ({
+  resolveAgentChannelStateDir: () => '/tmp/none',
+}))
+vi.mock('../web/agent-config.js', () => ({
+  readAgentProvider: (name: string) => name === 'codex-dev' ? 'codex' : 'claude',
+  readAgentRemoteHost: () => null,
+  readAgentVoiceConfig: () => ({ responseMode: 'text' }),
+}))
+vi.mock('../web/agent-process.js', () => ({
+  agentSessionName: (name: string) => `agent-${name}`,
+  isSessionReadyForPrompt: vi.fn(() => false),
+  clearStaleParkedInput: vi.fn(() => false),
+  sendPromptToSession: vi.fn(),
+  sessionExistsOnHost: vi.fn(() => false),
+}))
+vi.mock('../web/voice-modality.js', () => ({ setLastInboundModality: vi.fn() }))
+vi.mock('../web/main-agent.js', () => ({ MAIN_CHANNELS_SESSION: 'orin-channels' }))
+vi.mock('../web/agent-message-wrap.js', () => ({
+  classifyAgentMessage: () => ({ category: 'trusted-peer', safeFrom: 'orin' }),
+  wrapAgentMessageForDelivery: () => ({ prefix: 'PREFIX\n', wrapped: 'WRAPPED' }),
+}))
+vi.mock('../providers/codex-provider.js', () => ({
+  dispatchCodexRun: (...args: unknown[]) => mockDispatch(...args),
+}))
+vi.mock('../providers/codex-bridge-client.js', () => ({
+  CodexBridgeHttpError: class CodexBridgeHttpError extends Error {},
+}))
+
+import { runMessageRouterTick } from '../web/message-router.js'
+
+describe('Codex message routing', () => {
+  beforeEach(() => {
+    vi.clearAllMocks()
+    mockGetPending.mockReturnValue([{
+      id: 41,
+      from_agent: 'orin',
+      to_agent: 'codex-dev',
+      content: 'build it',
+      status: 'pending',
+      result: null,
+      created_at: Math.floor(Date.now() / 1000),
+      delivered_at: null,
+      completed_at: null,
+    }])
+    mockDispatch.mockResolvedValue({ runId: 'run-41', state: 'queued', duplicate: false })
+    mockMarkDelivered.mockReturnValue(true)
+  })
+
+  it('dispatches through the Bridge and marks the Béla message delivered', async () => {
+    await runMessageRouterTick()
+
+    expect(mockDispatch).toHaveBeenCalledWith('codex-dev', {
+      messageId: 41,
+      prompt: 'PREFIX\nWRAPPED',
+      context: {
+        belaMessageId: 41,
+        fromAgent: 'orin',
+        toAgent: 'codex-dev',
+      },
+    })
+    expect(mockMarkDelivered).toHaveBeenCalledWith(41)
+  })
+})
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/__tests__/codex-provider-callback-dedupe.test.ts marveen-1.21.1-0.1.7/src/__tests__/codex-provider-callback-dedupe.test.ts
--- marveen-1.21.1-current/src/__tests__/codex-provider-callback-dedupe.test.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/__tests__/codex-provider-callback-dedupe.test.ts	2026-07-28 22:45:41.867314244 +0900
@@ -0,0 +1,54 @@
+import { beforeEach, describe, expect, it, vi } from 'vitest'
+
+const dbGet = vi.hoisted(() => vi.fn())
+
+vi.mock('../db.js', () => ({
+  createAgentMessage: vi.fn(),
+  getAgentMessage: vi.fn(),
+  getDb: () => ({
+    prepare: vi.fn(() => ({ get: dbGet })),
+  }),
+  markMessageDone: vi.fn(),
+  markMessageFailed: vi.fn(),
+}))
+
+import {
+  CODEX_CALLBACK_ADAPTER_REVISION,
+  hasEquivalentAgentReply,
+} from '../web/routes/provider-callbacks.js'
+
+beforeEach(() => {
+  dbGet.mockReset()
+})
+
+describe('Codex provider callback reply deduplication', () => {
+  it('advertises callback adapter revision 2', () => {
+    expect(CODEX_CALLBACK_ADAPTER_REVISION).toBe(2)
+  })
+
+  it('finds only an exact reply from the same agent and target inside the run window', () => {
+    dbGet.mockReturnValueOnce({ id: 17 })
+    expect(hasEquivalentAgentReply(
+      'codex-dedupe',
+      'bela',
+      'EXACT_FINAL_REPLY',
+      100,
+    )).toBe(true)
+    expect(dbGet).toHaveBeenCalledWith(
+      'codex-dedupe',
+      'bela',
+      'EXACT_FINAL_REPLY',
+      100,
+    )
+  })
+
+  it('returns false when the scoped query finds no equivalent reply', () => {
+    dbGet.mockReturnValueOnce(undefined)
+    expect(hasEquivalentAgentReply(
+      'codex-dedupe',
+      'bela',
+      'EXACT_FINAL_REPLY',
+      101,
+    )).toBe(false)
+  })
+})
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/__tests__/message-router-tick-cap.test.ts marveen-1.21.1-0.1.7/src/__tests__/message-router-tick-cap.test.ts
--- marveen-1.21.1-current/src/__tests__/message-router-tick-cap.test.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/__tests__/message-router-tick-cap.test.ts	2026-07-28 15:40:20.306548195 +0900
@@ -37,6 +37,7 @@
 }))
 
 vi.mock('../web/agent-config.js', () => ({
+  readAgentProvider: () => 'claude',
   readAgentRemoteHost: () => null,
   readAgentVoiceConfig: () => ({ responseMode: 'text' }),
 }))
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/__tests__/provider-service-auth.test.ts marveen-1.21.1-0.1.7/src/__tests__/provider-service-auth.test.ts
--- marveen-1.21.1-current/src/__tests__/provider-service-auth.test.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/__tests__/provider-service-auth.test.ts	2026-07-28 15:40:20.306548195 +0900
@@ -0,0 +1,20 @@
+import { describe, expect, it } from 'vitest'
+import { isProviderServiceEndpoint } from '../web/provider-service-auth.js'
+
+describe('provider service auth boundary', () => {
+  it('allows only the Codex callback POST', () => {
+    expect(isProviderServiceEndpoint('/api/provider-callbacks/codex', 'POST')).toBe(true)
+    expect(isProviderServiceEndpoint('/api/provider-callbacks/codex', 'GET')).toBe(false)
+  })
+
+  it('allows scoped facade reads and writes', () => {
+    expect(isProviderServiceEndpoint('/api/codex-facade/messages', 'POST')).toBe(true)
+    expect(isProviderServiceEndpoint('/api/codex-facade/messages/12', 'GET')).toBe(true)
+  })
+
+  it('does not weaken unrelated dashboard routes', () => {
+    expect(isProviderServiceEndpoint('/api/agents', 'GET')).toBe(false)
+    expect(isProviderServiceEndpoint('/api/codex-facade/messages', 'DELETE')).toBe(false)
+    expect(isProviderServiceEndpoint('/api/provider-callbacks/claude', 'POST')).toBe(false)
+  })
+})
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/providers/codex-bridge-client.ts marveen-1.21.1-0.1.7/src/providers/codex-bridge-client.ts
--- marveen-1.21.1-current/src/providers/codex-bridge-client.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/providers/codex-bridge-client.ts	2026-07-28 15:40:20.306548195 +0900
@@ -0,0 +1,123 @@
+import { readFileSync } from 'node:fs'
+import http from 'node:http'
+import { homedir } from 'node:os'
+import { join } from 'node:path'
+
+const DEFAULT_SOCKET = process.env.BELA_CODEX_BRIDGE_SOCKET
+  || `/run/user/${process.getuid?.() ?? 1000}/bela-codex-bridge.sock`
+const DEFAULT_TOKEN_FILE = process.env.BELA_CODEX_BRIDGE_TOKEN_FILE
+  || join(homedir(), '.config', 'bela-codex-bridge', 'token')
+
+export class CodexBridgeHttpError extends Error {
+  constructor(
+    readonly status: number,
+    readonly code: string,
+    message: string,
+    readonly retryable: boolean,
+  ) {
+    super(message)
+    this.name = 'CodexBridgeHttpError'
+  }
+}
+
+interface BridgeEnvelope<T> {
+  data?: T
+  duplicate?: boolean
+  error?: { code?: string; message?: string }
+}
+
+export class CodexBridgeClient {
+  constructor(
+    private readonly socketPath = DEFAULT_SOCKET,
+    private readonly tokenFile = DEFAULT_TOKEN_FILE,
+    private readonly timeoutMs = 30_000,
+  ) {}
+
+  async request<T>(
+    method: string,
+    path: string,
+    body?: Record<string, unknown>,
+    extraHeaders: Record<string, string> = {},
+  ): Promise<{ data: T; duplicate: boolean }> {
+    let token: string
+    try {
+      token = readFileSync(this.tokenFile, 'utf8').trim()
+    } catch (error) {
+      throw new CodexBridgeHttpError(
+        503,
+        'bridge_token_unavailable',
+        `Codex Bridge token cannot be read: ${(error as Error).message}`,
+        true,
+      )
+    }
+    if (!token) {
+      throw new CodexBridgeHttpError(503, 'bridge_token_empty', 'Codex Bridge token file is empty', true)
+    }
+
+    const payload = body === undefined ? null : Buffer.from(JSON.stringify(body))
+    return new Promise((resolve, reject) => {
+      const req = http.request({
+        socketPath: this.socketPath,
+        path,
+        method,
+        headers: {
+          authorization: `Bearer ${token}`,
+          accept: 'application/json',
+          ...(payload
+            ? {
+                'content-type': 'application/json',
+                'content-length': String(payload.length),
+              }
+            : {}),
+          ...extraHeaders,
+        },
+      }, (res) => {
+        const chunks: Buffer[] = []
+        let bytes = 0
+        res.on('data', (chunk: Buffer) => {
+          bytes += chunk.length
+          if (bytes > 4 * 1024 * 1024) {
+            req.destroy(new Error('Codex Bridge response exceeds 4 MiB'))
+            return
+          }
+          chunks.push(chunk)
+        })
+        res.on('end', () => {
+          let parsed: BridgeEnvelope<T>
+          try {
+            parsed = JSON.parse(Buffer.concat(chunks).toString('utf8') || '{}') as BridgeEnvelope<T>
+          } catch {
+            reject(new CodexBridgeHttpError(
+              res.statusCode ?? 502,
+              'invalid_bridge_response',
+              'Codex Bridge returned invalid JSON',
+              true,
+            ))
+            return
+          }
+          const status = res.statusCode ?? 502
+          if (status < 200 || status >= 300 || parsed.data === undefined) {
+            const code = parsed.error?.code || `http_${status}`
+            reject(new CodexBridgeHttpError(
+              status,
+              code,
+              parsed.error?.message || `Codex Bridge HTTP ${status}`,
+              status >= 500 || status === 408 || status === 409 || status === 429,
+            ))
+            return
+          }
+          resolve({ data: parsed.data, duplicate: parsed.duplicate === true })
+        })
+      })
+      req.setTimeout(this.timeoutMs, () => req.destroy(new Error('Codex Bridge request timed out')))
+      req.on('error', (error) => {
+        if (error instanceof CodexBridgeHttpError) reject(error)
+        else reject(new CodexBridgeHttpError(503, 'bridge_unreachable', error.message, true))
+      })
+      if (payload) req.end(payload)
+      else req.end()
+    })
+  }
+}
+
+export const codexBridgeClient = new CodexBridgeClient()
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/providers/codex-provider.ts marveen-1.21.1-0.1.7/src/providers/codex-provider.ts
--- marveen-1.21.1-current/src/providers/codex-provider.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/providers/codex-provider.ts	2026-07-28 15:40:20.306548195 +0900
@@ -0,0 +1,160 @@
+import { readFileSync } from 'node:fs'
+import { join } from 'node:path'
+import {
+  agentDir,
+  readAgentCodexProviderConfig,
+} from '../web/agent-config.js'
+import { codexBridgeClient } from './codex-bridge-client.js'
+import type { ProviderDispatch, ProviderRun, ProviderStatus } from './types.js'
+
+const statusCache = new Map<string, ProviderStatus>()
+
+function readOptional(path: string): string {
+  try { return readFileSync(path, 'utf8') } catch { return '' }
+}
+
+function instructionsFor(agentId: string): string {
+  const dir = agentDir(agentId)
+  const identity = readOptional(join(dir, 'CLAUDE.md'))
+  const soul = readOptional(join(dir, 'SOUL.md'))
+  return [
+    'This agent is managed by Béla through the Codex Bridge.',
+    identity && `Béla agent identity:\n${identity}`,
+    soul && `Béla agent soul/personality:\n${soul}`,
+  ].filter(Boolean).join('\n\n')
+}
+
+export async function syncCodexAgent(agentId: string): Promise<void> {
+  const config = readAgentCodexProviderConfig(agentId)
+  await codexBridgeClient.request(
+    'PUT',
+    `/v1/agents/${encodeURIComponent(agentId)}`,
+    {
+      displayName: agentId,
+      model: config.model,
+      workspacePath: agentDir(agentId),
+      workspaceMode: 'directory',
+      sandboxMode: config.sandboxMode,
+      approvalPolicy: config.approvalPolicy,
+      networkEnabled: config.networkEnabled,
+      instructions: instructionsFor(agentId),
+    },
+  )
+}
+
+export async function startCodexAgent(agentId: string): Promise<ProviderStatus> {
+  await syncCodexAgent(agentId)
+  const result = await codexBridgeClient.request<Record<string, unknown>>(
+    'POST',
+    `/v1/agents/${encodeURIComponent(agentId)}/start`,
+  )
+  const status: ProviderStatus = {
+    provider: 'codex',
+    reachable: true,
+    configured: true,
+    desiredState: String(result.data.desiredState ?? 'running'),
+    actualState: String(result.data.actualState ?? 'idle'),
+    model: String(result.data.model ?? readAgentCodexProviderConfig(agentId).model),
+  }
+  statusCache.set(agentId, status)
+  return status
+}
+
+export async function stopCodexAgent(agentId: string): Promise<ProviderStatus> {
+  const result = await codexBridgeClient.request<Record<string, unknown>>(
+    'POST',
+    `/v1/agents/${encodeURIComponent(agentId)}/stop`,
+  )
+  const status: ProviderStatus = {
+    provider: 'codex',
+    reachable: true,
+    configured: true,
+    desiredState: String(result.data.desiredState ?? 'stopped'),
+    actualState: String(result.data.actualState ?? 'offline'),
+    model: String(result.data.model ?? readAgentCodexProviderConfig(agentId).model),
+  }
+  statusCache.set(agentId, status)
+  return status
+}
+
+export async function restartCodexAgent(agentId: string, fresh = false): Promise<ProviderStatus> {
+  await syncCodexAgent(agentId)
+  if (fresh) {
+    await codexBridgeClient.request(
+      'POST',
+      `/v1/agents/${encodeURIComponent(agentId)}/fresh-thread`,
+    )
+  }
+  const result = await codexBridgeClient.request<Record<string, unknown>>(
+    'POST',
+    `/v1/agents/${encodeURIComponent(agentId)}/restart`,
+  )
+  const status: ProviderStatus = {
+    provider: 'codex',
+    reachable: true,
+    configured: true,
+    desiredState: String(result.data.desiredState ?? 'running'),
+    actualState: String(result.data.actualState ?? 'idle'),
+    model: String(result.data.model ?? readAgentCodexProviderConfig(agentId).model),
+  }
+  statusCache.set(agentId, status)
+  return status
+}
+
+export async function getCodexAgentStatus(agentId: string): Promise<ProviderStatus> {
+  try {
+    const result = await codexBridgeClient.request<{
+      desiredState?: string
+      actualState?: string
+      model?: string
+    }>('GET', `/v1/agents/${encodeURIComponent(agentId)}`)
+    const status: ProviderStatus = {
+      provider: 'codex',
+      reachable: true,
+      configured: true,
+      desiredState: result.data.desiredState,
+      actualState: result.data.actualState,
+      model: result.data.model,
+    }
+    statusCache.set(agentId, status)
+    return status
+  } catch (error) {
+    const err = error as Error & { status?: number }
+    const status: ProviderStatus = {
+      provider: 'codex',
+      reachable: err.status === 404,
+      configured: false,
+      error: err.message,
+    }
+    statusCache.set(agentId, status)
+    return status
+  }
+}
+
+export function getCachedCodexAgentStatus(agentId: string): ProviderStatus | null {
+  return statusCache.get(agentId) ?? null
+}
+
+export async function dispatchCodexRun(
+  agentId: string,
+  dispatch: ProviderDispatch,
+): Promise<ProviderRun> {
+  const result = await codexBridgeClient.request<{
+    runId: string
+    state: string
+  }>(
+    'POST',
+    `/v1/agents/${encodeURIComponent(agentId)}/runs`,
+    {
+      prompt: dispatch.prompt,
+      context: dispatch.context,
+      priority: 0,
+    },
+    { 'idempotency-key': `bela-message-${dispatch.messageId}` },
+  )
+  return {
+    runId: result.data.runId,
+    state: result.data.state,
+    duplicate: result.duplicate,
+  }
+}
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/providers/types.ts marveen-1.21.1-0.1.7/src/providers/types.ts
--- marveen-1.21.1-current/src/providers/types.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/providers/types.ts	2026-07-28 15:40:20.306548195 +0900
@@ -0,0 +1,30 @@
+export type AgentProvider = 'claude' | 'codex'
+
+export interface CodexProviderConfig {
+  model: string
+  sandboxMode: 'read-only' | 'workspace-write' | 'danger-full-access'
+  approvalPolicy: 'never' | 'bela'
+  networkEnabled: boolean
+}
+
+export interface ProviderDispatch {
+  messageId: number
+  prompt: string
+  context: Record<string, unknown>
+}
+
+export interface ProviderRun {
+  runId: string
+  state: string
+  duplicate: boolean
+}
+
+export interface ProviderStatus {
+  provider: AgentProvider
+  reachable: boolean
+  configured: boolean
+  desiredState?: string
+  actualState?: string
+  model?: string
+  error?: string
+}
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/agent-config.ts marveen-1.21.1-0.1.7/src/web/agent-config.ts
--- marveen-1.21.1-current/src/web/agent-config.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/agent-config.ts	2026-07-28 15:40:20.306548195 +0900
@@ -4,6 +4,7 @@
 import { PROJECT_ROOT, MAIN_AGENT_ID } from '../config.js'
 import { atomicWriteFileSync } from './atomic-write.js'
 import { safeJoin } from './sanitize.js'
+import type { AgentProvider, CodexProviderConfig } from '../providers/types.js'
 
 export const AGENTS_BASE_DIR = join(PROJECT_ROOT, 'agents')
 
@@ -72,6 +73,90 @@
   atomicWriteFileSync(configPath, JSON.stringify(config, null, 2))
 }
 
+// Provider selection is stored next to the existing per-agent model config so
+// a Béla update cannot split identity and runtime choice across two stores.
+// Missing/invalid values deliberately fall back to Claude for backwards
+// compatibility with every agent created before the Bridge existed.
+export function readAgentProvider(name: string): AgentProvider {
+  const configPath = join(agentDir(name), 'agent-config.json')
+  try {
+    const config = JSON.parse(readFileOr(configPath, '{}')) as Record<string, unknown>
+    return config.provider === 'codex' ? 'codex' : 'claude'
+  } catch {
+    return 'claude'
+  }
+}
+
+export function writeAgentProvider(name: string, provider: AgentProvider): void {
+  const configPath = join(agentDir(name), 'agent-config.json')
+  let config: Record<string, unknown> = {}
+  try { config = JSON.parse(readFileOr(configPath, '{}')) } catch {}
+  config.provider = provider
+  atomicWriteFileSync(configPath, JSON.stringify(config, null, 2))
+}
+
+const CODEX_SANDBOXES = new Set<CodexProviderConfig['sandboxMode']>([
+  'read-only',
+  'workspace-write',
+  'danger-full-access',
+])
+const CODEX_APPROVALS = new Set<CodexProviderConfig['approvalPolicy']>(['never', 'bela'])
+
+export function readAgentCodexProviderConfig(name: string): CodexProviderConfig {
+  const configPath = join(agentDir(name), 'agent-config.json')
+  try {
+    const config = JSON.parse(readFileOr(configPath, '{}')) as Record<string, unknown>
+    const raw = config.providerConfig && typeof config.providerConfig === 'object'
+      ? config.providerConfig as Record<string, unknown>
+      : {}
+    const sandbox = typeof raw.sandboxMode === 'string'
+      && CODEX_SANDBOXES.has(raw.sandboxMode as CodexProviderConfig['sandboxMode'])
+      ? raw.sandboxMode as CodexProviderConfig['sandboxMode']
+      : 'workspace-write'
+    const approval = typeof raw.approvalPolicy === 'string'
+      && CODEX_APPROVALS.has(raw.approvalPolicy as CodexProviderConfig['approvalPolicy'])
+      ? raw.approvalPolicy as CodexProviderConfig['approvalPolicy']
+      : 'bela'
+    return {
+      model: typeof raw.model === 'string' && raw.model.trim()
+        ? raw.model.trim()
+        : 'gpt-5.6-terra',
+      sandboxMode: sandbox,
+      approvalPolicy: approval,
+      networkEnabled: raw.networkEnabled === true,
+    }
+  } catch {
+    return {
+      model: 'gpt-5.6-terra',
+      sandboxMode: 'workspace-write',
+      approvalPolicy: 'bela',
+      networkEnabled: false,
+    }
+  }
+}
+
+export function writeAgentCodexProviderConfig(
+  name: string,
+  value: Partial<CodexProviderConfig>,
+): void {
+  const configPath = join(agentDir(name), 'agent-config.json')
+  let config: Record<string, unknown> = {}
+  try { config = JSON.parse(readFileOr(configPath, '{}')) } catch {}
+  const current = readAgentCodexProviderConfig(name)
+  const next: CodexProviderConfig = {
+    model: typeof value.model === 'string' && value.model.trim() ? value.model.trim() : current.model,
+    sandboxMode: value.sandboxMode && CODEX_SANDBOXES.has(value.sandboxMode)
+      ? value.sandboxMode
+      : current.sandboxMode,
+    approvalPolicy: value.approvalPolicy && CODEX_APPROVALS.has(value.approvalPolicy)
+      ? value.approvalPolicy
+      : current.approvalPolicy,
+    networkEnabled: value.networkEnabled ?? current.networkEnabled,
+  }
+  config.providerConfig = next
+  atomicWriteFileSync(configPath, JSON.stringify(config, null, 2))
+}
+
 export function readAgentDisplayName(name: string): string {
   const configPath = join(agentDir(name), 'agent-config.json')
   try {
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/agent-process.ts marveen-1.21.1-0.1.7/src/web/agent-process.ts
--- marveen-1.21.1-current/src/web/agent-process.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/agent-process.ts	2026-07-28 15:40:20.306548195 +0900
@@ -16,7 +16,7 @@
   paneShowsContextSaturation,
   idleConsideringDimGhost,
 } from '../pane-state.js'
-import { agentDir, listAgentNames, readAgentModel, readAgentClaudeConfigDir, readAgentChannelProvider, readAgentAuthMode, readAgentDisplayName, readAgentRemoteConfig, readAgentRemoteHost, readAgentMemoryIsolation } from './agent-config.js'
+import { agentDir, listAgentNames, readAgentModel, readAgentProvider, readAgentClaudeConfigDir, readAgentChannelProvider, readAgentAuthMode, readAgentDisplayName, readAgentRemoteConfig, readAgentRemoteHost, readAgentMemoryIsolation } from './agent-config.js'
 import { provisionMemoryBoundaryDir } from './memory-boundary.js'
 import { renameSharedCredentialsIfSafe } from './claude-credentials-guard.js'
 import {
@@ -510,6 +510,9 @@
 export function startAgentProcess(name: string, opts: { fresh?: boolean } = {}): { ok: boolean; pid?: number; error?: string } {
   const dir = agentDir(name)
   if (!existsSync(dir)) return { ok: false, error: 'Agent not found' }
+  if (readAgentProvider(name) === 'codex') {
+    return { ok: false, error: 'Codex agents must be started through the Codex Bridge provider' }
+  }
 
   // Remote agents are handled entirely by the ssh path above (with its own
   // start guard), before any local already-running check / scaffolding.
@@ -1458,4 +1461,3 @@
   logger.warn({ session, parked: parked.slice(0, 60) }, 'message-router: cleared stale parked input (channel un-wedge)')
   return true
 }
-
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/agent-scaffold.ts marveen-1.21.1-0.1.7/src/web/agent-scaffold.ts
--- marveen-1.21.1-current/src/web/agent-scaffold.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/agent-scaffold.ts	2026-07-28 15:40:20.306548195 +0900
@@ -362,6 +362,99 @@
   }
 }
 
+export interface AgentIdentityFiles {
+  claudeMd: string
+  soulMd: string
+}
+
+interface AgentIdentityGenerators {
+  claudeMd: (name: string, description: string, model: string) => Promise<string>
+  soulMd: (name: string, description: string) => Promise<string>
+}
+
+/**
+ * Codex agents must be creatable without a Claude worker or Anthropic API.
+ * Marveen still stores the provider-neutral identity in CLAUDE.md/SOUL.md for
+ * dashboard and fleet compatibility; the Codex provider compiles their content
+ * into the Bridge's AGENTS.md developer instructions before the first turn.
+ */
+export function buildCodexAgentIdentityFiles(name: string, description: string): AgentIdentityFiles {
+  const cleanName = name.trim()
+  const cleanDescription = description.trim()
+  const claudeMd = `# ${cleanName}
+
+> ${cleanDescription}
+
+## Szerep és felelősség
+
+- Elsődleges feladatod: ${cleanDescription}
+- Béla alárendelt Codex-agentjeként dolgozol; Béla a főagent és koordinátor.
+- A kapott feladatot a rendelkezésre álló kontextus és eszközök alapján hajtsd végre.
+- Ne találj ki adatot, eredményt vagy sikeres műveletet. A bizonytalanságot egyértelműen jelezd.
+
+## Működési szabályok
+
+- Csak a Codex Bridge által kijelölt munkakönyvtárban dolgozz.
+- Külső vagy visszafordíthatatlan műveletet csak a beállított jóváhagyási szabály szerint végezz.
+- Más agent nevében ne kommunikálj, és más agent azonosítóját ne használd.
+- Ha a feladat Bélától kapott kiegészítő döntést vagy jogosultságot igényel, küldj neki agentüzenetet.
+- A Béla MCP-eszközöket kizárólag a saját agentazonosítóddal használd.
+
+## Kommunikáció
+
+- ${OWNER_NAME} számára alapértelmezetten magyarul, világosan és tárgyilagosan válaszolj.
+- Kód, parancsok, azonosítók és szabványos technikai kifejezések maradhatnak angolul.
+- Először az eredményt közöld, majd csak a döntéshez vagy ellenőrzéshez szükséges részleteket.
+- Ne állítsd, hogy egy művelet elkészült, amíg azt ellenőrizhetően végre nem hajtottad.
+
+## Béla-kapcsolat és memória
+
+- Másik agentnek a \`bela_agent_message_send\` eszközzel küldj üzenetet.
+- Az elküldött üzenet állapotát a \`bela_agent_message_status\` eszközzel ellenőrizheted.
+- Releváns korábbi információ kereséséhez használd a \`bela_memory_search\`, részletes beolvasásához a \`bela_memory_get\` eszközt.
+- Ha olyan képesség kell, amelyet a Bridge nem adott át, jelezd ${BOT_NAME} főagentnek (${MAIN_AGENT_ID}); ne próbáld megkerülni a korlátozást.
+`
+  const soulMd = `# ${cleanName} személyisége
+
+## Alapkarakter
+
+- Megbízható, precíz és feladatorientált.
+- Önállóan gondolkodik, de elfogadja, hogy Béla a koordináló főagent.
+- Nem szépíti a hibákat és nem leplezi a bizonytalanságot.
+
+## Kommunikációs stílus
+
+- Tömör, világos és szakmai.
+- ${OWNER_NAME} felé magyarul kommunikál, hacsak a feladat más nyelvet nem kíván.
+- A fontos kockázatokat, hiányzó adatokat és ellenőrizetlen feltételezéseket külön jelzi.
+
+## Kerülendő viselkedés
+
+- Fiktív eredmény vagy elvégzett művelet állítása.
+- Indokolatlanul hosszú, ismétlődő válasz.
+- Más agent megszemélyesítése vagy a jóváhagyási rendszer megkerülése.
+`
+  return { claudeMd, soulMd }
+}
+
+export async function generateAgentIdentityFiles(
+  provider: 'claude' | 'codex',
+  name: string,
+  description: string,
+  model: string,
+  generators: AgentIdentityGenerators = {
+    claudeMd: generateClaudeMd,
+    soulMd: generateSoulMd,
+  },
+): Promise<AgentIdentityFiles> {
+  if (provider === 'codex') return buildCodexAgentIdentityFiles(name, description)
+  const [claudeMd, soulMd] = await Promise.all([
+    generators.claudeMd(name, description, model),
+    generators.soulMd(name, description),
+  ])
+  return { claudeMd, soulMd }
+}
+
 export async function generateClaudeMd(name: string, description: string, model: string): Promise<string> {
   // Distribution-safe default-drive line: only emit a concrete folder when this
   // install has one configured (OWNER_DRIVE_FOLDER). A fresh install with no
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/auto-restart-runner.ts marveen-1.21.1-0.1.7/src/web/auto-restart-runner.ts
--- marveen-1.21.1-current/src/web/auto-restart-runner.ts	2026-06-23 22:16:07.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/auto-restart-runner.ts	2026-07-28 15:40:20.306548195 +0900
@@ -1,7 +1,7 @@
 import { execFileSync } from 'node:child_process'
 import { logger } from '../logger.js'
 import { MAIN_AGENT_ID, SERVICE_ID } from '../config.js'
-import { listAgentNames, readAgentRemoteHost } from './agent-config.js'
+import { listAgentNames, readAgentProvider, readAgentRemoteHost } from './agent-config.js'
 import {
   agentRunState,
   agentSessionName,
@@ -77,6 +77,7 @@
 }
 
 function checkAgent(name: string, nowMs: number): void {
+  if (name !== MAIN_AGENT_ID && readAgentProvider(name) === 'codex') return
   const cfg = readAutoRestartConfig(name)
   if (!cfg.enabled) {
     lastRestart.delete(name) // re-seed cleanly if re-enabled later
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/channel-monitor.ts marveen-1.21.1-0.1.7/src/web/channel-monitor.ts
--- marveen-1.21.1-current/src/web/channel-monitor.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/channel-monitor.ts	2026-07-28 15:40:20.306548195 +0900
@@ -5,7 +5,7 @@
 import { resolveFromPath } from '../platform.js'
 import { logger } from '../logger.js'
 import { MAIN_AGENT_ID, SERVICE_ID, BOT_NAME, CHANNEL_PROVIDER, PROJECT_ROOT, RESPAWN_ENABLED } from '../config.js'
-import { agentDir, listAgentNames, readAgentChannelProvider } from './agent-config.js'
+import { agentDir, listAgentNames, readAgentChannelProvider, readAgentProvider } from './agent-config.js'
 import {
   agentHasChannel,
   agentSessionName,
@@ -1449,7 +1449,9 @@
   if (reconcileBurstInProgress) return
   const desired = getDesiredAgents()
   if (desired.size === 0) return
-  const down = [...desired].filter((name) => !isAgentRunning(name))
+  // Codex agents are reconciled by the standalone Bridge. Starting them here
+  // would accidentally launch Claude/tmux with a Codex model id.
+  const down = [...desired].filter((name) => readAgentProvider(name) === 'claude' && !isAgentRunning(name))
   if (down.length === 0) return
   reconcileBurstInProgress = true
   try {
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/message-router.ts marveen-1.21.1-0.1.7/src/web/message-router.ts
--- marveen-1.21.1-current/src/web/message-router.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/message-router.ts	2026-07-28 15:40:20.306548195 +0900
@@ -2,11 +2,13 @@
 import { MAIN_AGENT_ID } from '../config.js'
 import { resolveAgentChannelStateDir } from './voice-directive.js'
 import {
+  createAgentMessage,
   getPendingMessages,
   markMessageDelivered,
   markMessageFailed,
+  type AgentMessage,
 } from '../db.js'
-import { readAgentRemoteHost, readAgentVoiceConfig } from './agent-config.js'
+import { readAgentProvider, readAgentRemoteHost, readAgentVoiceConfig } from './agent-config.js'
 import {
   agentSessionName,
   isSessionReadyForPrompt,
@@ -17,6 +19,8 @@
 import { setLastInboundModality } from './voice-modality.js'
 import { classifyAgentMessage, wrapAgentMessageForDelivery } from './agent-message-wrap.js'
 import { MAIN_CHANNELS_SESSION } from './main-agent.js'
+import { dispatchCodexRun } from '../providers/codex-provider.js'
+import { CodexBridgeHttpError } from '../providers/codex-bridge-client.js'
 
 // A message that cannot be delivered within this window (target session never
 // exists / stays busy) is marked failed so it stops clogging the pending
@@ -64,6 +68,93 @@
 
 // Max messages drained per 5s tick; a larger backlog rolls to the next tick.
 export const MAX_MESSAGES_PER_TICK = 25
+const MAX_CODEX_PER_TICK = 10
+
+// Codex agents are durable Bridge queues rather than tmux panes. The message
+// remains pending while the Bridge is unavailable, is marked delivered after
+// the idempotent run is accepted, and is completed by the authenticated
+// provider callback.
+async function deliverCodexBatch(messages: AgentMessage[], now: number): Promise<void> {
+  for (const msg of messages.slice(0, MAX_CODEX_PER_TICK)) {
+    const ageMs = now - msg.created_at * 1000
+    try {
+      const cls = classifyAgentMessage(msg.from_agent, msg.to_agent)
+      if (!cls) {
+        markMessageFailed(msg.id, 'Invalid or empty from_agent')
+        continue
+      }
+
+      const { category, safeFrom } = cls
+      const isChannelInbound = category === 'channel-inbound'
+      let deliveryContent = msg.content
+      if (isChannelInbound) {
+        const voiceFileId = extractVoiceFileId(msg.content)
+        const chatId = extractChatId(msg.content)
+        if (voiceFileId && chatId) {
+          setLastInboundModality(msg.to_agent, chatId, 'voice')
+          const voiceCfg = readAgentVoiceConfig(msg.to_agent)
+          if (voiceCfg.responseMode !== 'text') {
+            const transcript = await callVoiceSTT(voiceFileId, msg.to_agent)
+            if (transcript) deliveryContent = injectTranscript(msg.content, transcript)
+          }
+        } else if (chatId) {
+          setLastInboundModality(msg.to_agent, chatId, 'text')
+        }
+      }
+
+      const { prefix, wrapped } = wrapAgentMessageForDelivery(
+        category,
+        safeFrom,
+        msg.from_agent,
+        isChannelInbound ? deliveryContent : msg.content,
+        msg.id,
+      )
+      const run = await dispatchCodexRun(msg.to_agent, {
+        messageId: msg.id,
+        prompt: prefix + wrapped,
+        context: {
+          belaMessageId: msg.id,
+          fromAgent: msg.from_agent,
+          toAgent: msg.to_agent,
+        },
+      })
+      if (!markMessageDelivered(msg.id)) {
+        logger.warn({ id: msg.id, runId: run.runId }, 'Codex message was no longer pending after run acceptance')
+      }
+      routerLoggedMisses.delete(msg.id)
+      logger.info({
+        id: msg.id,
+        from: msg.from_agent,
+        to: msg.to_agent,
+        runId: run.runId,
+        duplicate: run.duplicate,
+      }, 'Agent message accepted by Codex Bridge')
+    } catch (error) {
+      const bridgeError = error instanceof CodexBridgeHttpError ? error : null
+      const terminal = bridgeError ? !bridgeError.retryable : false
+      if (terminal || ageMs > MESSAGE_ABANDON_WINDOW_MS) {
+        const reason = bridgeError
+          ? `${bridgeError.code}: ${bridgeError.message}`
+          : `Codex Bridge unavailable: ${(error as Error).message}`
+        markMessageFailed(msg.id, reason.slice(0, 2000))
+        createAgentMessage(
+          'system',
+          MAIN_AGENT_ID,
+          `[codex-bridge-failure] ${msg.to_agent} nem tudta átvenni a(z) #${msg.id} üzenetet: ${reason.slice(0, 500)}`,
+        )
+        routerLoggedMisses.delete(msg.id)
+      } else if (!routerLoggedMisses.has(msg.id)) {
+        logger.warn({
+          id: msg.id,
+          to: msg.to_agent,
+          code: bridgeError?.code,
+          err: error,
+        }, 'Codex Bridge delivery unavailable, will retry')
+        routerLoggedMisses.add(msg.id)
+      }
+    }
+  }
+}
 
 export function startMessageRouter(): NodeJS.Timeout {
   return setInterval(async () => {
@@ -89,8 +180,19 @@
     // backlog (e.g. after a delivery stall) can never make one tick run long
     // and starve the event loop -- the slow-tick half of the progressive-hang
     // pattern. Ordering is preserved (oldest first) so nothing is starved.
-    const pending = getPendingMessages().slice(0, MAX_MESSAGES_PER_TICK)
+    const allPending = getPendingMessages()
+    const codexPending: AgentMessage[] = []
+    const localPending: AgentMessage[] = []
+    for (const message of allPending) {
+      if (
+        message.to_agent !== MAIN_AGENT_ID
+        && readAgentProvider(message.to_agent) === 'codex'
+      ) codexPending.push(message)
+      else localPending.push(message)
+    }
+    const pending = localPending.slice(0, MAX_MESSAGES_PER_TICK)
     const now = Date.now()
+    await deliverCodexBatch(codexPending, now)
     let mainAgentWakeupFiredThisTick = false
     for (const msg of pending) {
       const ageMs = now - msg.created_at * 1000
@@ -294,4 +396,3 @@
     return null
   }
 }
-
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/model-fallback-runner.ts marveen-1.21.1-0.1.7/src/web/model-fallback-runner.ts
--- marveen-1.21.1-current/src/web/model-fallback-runner.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/model-fallback-runner.ts	2026-07-28 15:40:20.306548195 +0900
@@ -8,6 +8,7 @@
   listAgentNames,
   readAgentRemoteHost,
   readAgentModel,
+  readAgentProvider,
   writeAgentModel,
   resolveModelId,
   DEFAULT_MODEL,
@@ -89,6 +90,7 @@
 }
 
 function checkAgent(name: string, nowMs: number, revertAfterMs: number, chain: string[]): void {
+  if (name !== MAIN_AGENT_ID && readAgentProvider(name) === 'codex') return
   // Sub-agents must be up; the main session is launchd-managed (always present).
   if (name !== MAIN_AGENT_ID && agentRunState(name) !== 'running') return
 
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/provider-service-auth.ts marveen-1.21.1-0.1.7/src/web/provider-service-auth.ts
--- marveen-1.21.1-current/src/web/provider-service-auth.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/provider-service-auth.ts	2026-07-28 15:40:20.306548195 +0900
@@ -0,0 +1,8 @@
+// Provider callbacks and the agent-scoped MCP facade authenticate themselves
+// with Bridge-specific bearer tokens. They must bypass the dashboard token
+// gate, but only for the exact methods and paths owned by those handlers.
+export function isProviderServiceEndpoint(path: string, method: string): boolean {
+  if (path === '/api/provider-callbacks/codex') return method === 'POST'
+  if (!path.startsWith('/api/codex-facade/')) return false
+  return method === 'GET' || method === 'POST'
+}
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/routes/agents.ts marveen-1.21.1-0.1.7/src/web/routes/agents.ts
--- marveen-1.21.1-current/src/web/routes/agents.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/routes/agents.ts	2026-07-28 15:40:20.306548195 +0900
@@ -18,6 +18,10 @@
   resolveModelId,
   readAgentModel,
   writeAgentModel,
+  readAgentProvider,
+  writeAgentProvider,
+  readAgentCodexProviderConfig,
+  writeAgentCodexProviderConfig,
   readAgentDisplayName,
   writeAgentDisplayName,
   readAgentSecurityProfile,
@@ -76,8 +80,7 @@
 import {
   writeAgentSettingsFromProfile,
   scaffoldAgentDir,
-  generateClaudeMd,
-  generateSoulMd,
+  generateAgentIdentityFiles,
 } from '../agent-scaffold.js'
 import {
   isAgentRunning,
@@ -113,6 +116,15 @@
 import { suggestForAgent, type AgentSignals } from '../model-suggest.js'
 import { getTokenSummary } from '../token-usage.js'
 import { listScheduledTasks } from '../scheduled-tasks-io.js'
+import type { AgentProvider, CodexProviderConfig } from '../../providers/types.js'
+import { codexBridgeClient } from '../../providers/codex-bridge-client.js'
+import {
+  getCachedCodexAgentStatus,
+  getCodexAgentStatus,
+  restartCodexAgent,
+  startCodexAgent,
+  stopCodexAgent,
+} from '../../providers/codex-provider.js'
 
 const VALID_PROVIDERS = new Set<ChannelProviderType>(['telegram', 'slack', 'discord', 'googlechat', 'teams'])
 
@@ -304,6 +316,8 @@
   displayName: string
   description: string
   model: string
+  provider: AgentProvider
+  providerConfig: CodexProviderConfig | null
   activeModel: string | null
   runningSince: number | null
   authMode: AuthMode
@@ -359,22 +373,35 @@
   // never blocks on a sleeping laptop's ssh timeout. `running` is derived from
   // it; `unreachable` reads as not-running but is surfaced distinctly so the UI
   // does not show a still-alive remote agent as "stopped".
+  const provider = readAgentProvider(name)
   const remote = readAgentRemoteConfig(name)
-  const runState = agentRunStateCached(name, remote.host != null)
+  const codexStatus = provider === 'codex' ? getCachedCodexAgentStatus(name) : null
+  const codexRunning = codexStatus?.actualState === 'idle'
+    || codexStatus?.actualState === 'busy'
+    || codexStatus?.actualState === 'waiting_approval'
+  const runState: AgentRunState = provider === 'codex'
+    ? codexRunning ? 'running' : 'stopped'
+    : agentRunStateCached(name, remote.host != null)
   const running = runState === 'running'
-  const session = running ? agentSessionName(name) : undefined
-  const runningSince = running ? getAgentRunningSince(name) : null
+  const session = provider === 'claude' && running ? agentSessionName(name) : undefined
+  const runningSince = provider === 'claude' && running ? getAgentRunningSince(name) : null
 
   // Reauth badge: only meaningful for a running session (a stopped agent has
   // no pane to inspect). One capture-pane per running agent on the list poll.
-  const reauth = running ? detectReauthNeeded(capturePane(agentSessionName(name))) : { needsReauth: false }
+  const reauth = provider === 'claude' && running
+    ? detectReauthNeeded(capturePane(agentSessionName(name)))
+    : { needsReauth: false }
 
   return {
     name,
     displayName: readAgentDisplayName(name),
     description: extractDescriptionFromClaudeMd(claudeMd),
-    model: readAgentModel(name),
-    activeModel: running ? readActiveModelFromProjectDir(dir, runningSince ?? undefined, readAgentClaudeConfigDir(name) ?? undefined) : null,
+    model: provider === 'codex' ? readAgentCodexProviderConfig(name).model : readAgentModel(name),
+    provider,
+    providerConfig: provider === 'codex' ? readAgentCodexProviderConfig(name) : null,
+    activeModel: provider === 'codex'
+      ? codexStatus?.model ?? readAgentCodexProviderConfig(name).model
+      : running ? readActiveModelFromProjectDir(dir, runningSince ?? undefined, readAgentClaudeConfigDir(name) ?? undefined) : null,
     runningSince,
     authMode: readAgentAuthMode(name),
     securityProfile: readAgentSecurityProfile(name),
@@ -392,7 +419,9 @@
     session,
     hasAvatar: findAvatarForAgent(name) !== null,
     autoRestart: readAutoRestartConfig(name),
-    contextTokens: running ? readContextTokensFromProjectDir(dir, readAgentClaudeConfigDir(name) ?? undefined) : null,
+    contextTokens: provider === 'claude' && running
+      ? readContextTokensFromProjectDir(dir, readAgentClaudeConfigDir(name) ?? undefined)
+      : null,
     needsReauth: reauth.needsReauth,
     reauthReason: reauth.reason,
   }
@@ -452,6 +481,13 @@
   if (path === '/api/models/available' && method === 'GET') {
     const hasDeepseek = getSecret('DEEPSEEK_API_KEY') !== null
     json(res, {
+      providers: [
+        { id: 'claude', label: 'Claude Code' },
+        { id: 'codex', label: 'OpenAI Codex Bridge' },
+      ],
+      codex: [
+        { id: 'gpt-5.6-terra', label: 'GPT-5.6 Terra (ChatGPT előfizetés)' },
+      ],
       claude: [
         { id: 'claude-fable-5', label: 'Fable 5 (legújabb)' },
         { id: 'claude-opus-4-8[1m]', label: 'Opus 4.8 (1M kontextus, alapértelmezett)' },
@@ -470,6 +506,8 @@
   }
 
   if (path === '/api/agents' && method === 'GET') {
+    const codexNames = listAgentNames().filter(name => readAgentProvider(name) === 'codex')
+    await Promise.allSettled(codexNames.map(name => getCodexAgentStatus(name)))
     json(res, listAgentSummaries())
     return true
   }
@@ -514,6 +552,20 @@
     }
 
     for (const name of listAgentNames()) {
+      if (readAgentProvider(name) === 'codex') {
+        const status = getCachedCodexAgentStatus(name)
+        const running = status?.actualState === 'idle'
+          || status?.actualState === 'busy'
+          || status?.actualState === 'waiting_approval'
+        entries.push({
+          name,
+          isMain: false,
+          running,
+          state: status?.actualState ?? (status?.reachable === false ? 'unreachable' : 'stopped'),
+          tail: [],
+        })
+        continue
+      }
       // Remote agents: resolve run state + pane through the short-TTL caches so
       // this 3s-polled endpoint never blocks the event loop on an ssh timeout.
       const host = readAgentRemoteHost(name)
@@ -625,10 +677,27 @@
   if (path === '/api/agents' && method === 'POST') {
     const body = await readBody(req)
     const data = JSON.parse(body.toString())
-    const { description, model: rawModel, profile: rawProfile } = data as { name: string; description: string; model?: string; profile?: string }
+    const {
+      description,
+      model: rawModel,
+      profile: rawProfile,
+      provider: rawProvider,
+      providerConfig: rawProviderConfig,
+    } = data as {
+      name: string
+      description: string
+      model?: string
+      profile?: string
+      provider?: AgentProvider
+      providerConfig?: Partial<CodexProviderConfig>
+    }
     const rawName = typeof data.name === 'string' ? data.name.trim() : ''
     const name = sanitizeAgentName(rawName)
-    const model = resolveModelId(rawModel || DEFAULT_MODEL)
+    const provider: AgentProvider = rawProvider === 'codex' ? 'codex' : 'claude'
+    const model = provider === 'codex'
+      ? rawModel?.trim() || rawProviderConfig?.model?.trim() || 'gpt-5.6-terra'
+      : resolveModelId(rawModel || DEFAULT_MODEL)
+    const generationModel = provider === 'codex' ? DEFAULT_MODEL : model
     const profileId = (rawProfile || 'default').trim() || 'default'
 
     if (!name) { json(res, { error: 'Name is required' }, 400); return true }
@@ -637,16 +706,25 @@
 
     scaffoldAgentDir(name)
     writeAgentModel(name, model)
+    writeAgentProvider(name, provider)
+    if (provider === 'codex') {
+      writeAgentCodexProviderConfig(name, {
+        ...rawProviderConfig,
+        model,
+      })
+    }
     writeAgentSecurityProfile(name, profileId)
     writeAgentSettingsFromProfile(name, loadProfileTemplate(profileId))
     if (rawName && rawName !== name) writeAgentDisplayName(name, rawName)
 
-    logger.info({ name, description }, 'Generating agent CLAUDE.md and SOUL.md...')
+    logger.info({ name, description, provider }, 'Generating agent identity files...')
     try {
-      const [claudeMd, soulMd] = await Promise.all([
-        generateClaudeMd(name, description, model),
-        generateSoulMd(name, description),
-      ])
+      const { claudeMd, soulMd } = await generateAgentIdentityFiles(
+        provider,
+        name,
+        description,
+        generationModel,
+      )
       atomicWriteFileSync(join(agentDir(name), 'CLAUDE.md'), claudeMd)
       atomicWriteFileSync(join(agentDir(name), 'SOUL.md'), soulMd)
       logger.info({ name }, 'Agent created successfully')
@@ -1399,6 +1477,10 @@
   if (authInitMatch && method === 'POST') {
     const name = decodeURIComponent(authInitMatch[1])
     if (!existsSync(agentDir(name))) { json(res, { error: 'Agent not found' }, 404); return true }
+    if (readAgentProvider(name) === 'codex') {
+      json(res, { error: 'A Codex hitelesítést a Bridge szolgáltatás kezeli.' }, 400)
+      return true
+    }
     if (!isAgentRunning(name)) { json(res, { error: 'Agent is not running' }, 400); return true }
     const session = agentSessionName(name)
     const host = readAgentRemoteHost(name)
@@ -1439,6 +1521,17 @@
     // the --channels plugin MCP server (agent comes up deaf).
     let startFresh = false
     try { startFresh = JSON.parse((await readBody(req)).toString() || '{}').fresh === true } catch {}
+    if (readAgentProvider(name) === 'codex') {
+      try {
+        const status = await startCodexAgent(name)
+        addDesiredAgent(name)
+        json(res, { ok: true, provider: 'codex', status })
+      } catch (err) {
+        logger.error({ err, name }, 'Codex agent start failed')
+        json(res, { error: err instanceof Error ? err.message : 'Codex Bridge start failed' }, 502)
+      }
+      return true
+    }
     const result = startAgentProcess(name, { fresh: startFresh })
     // Record operator intent so the monitor keeps this agent up across shared
     // tmux-server restarts / reboots (see agent-desired-state.ts).
@@ -1451,6 +1544,17 @@
   const stopMatch = path.match(/^\/api\/agents\/([^/]+)\/stop$/)
   if (stopMatch && method === 'POST') {
     const name = decodeURIComponent(stopMatch[1])
+    if (readAgentProvider(name) === 'codex') {
+      try {
+        const status = await stopCodexAgent(name)
+        removeDesiredAgent(name)
+        json(res, { ok: true, provider: 'codex', status })
+      } catch (err) {
+        logger.error({ err, name }, 'Codex agent stop failed')
+        json(res, { error: err instanceof Error ? err.message : 'Codex Bridge stop failed' }, 502)
+      }
+      return true
+    }
     const result = stopAgentProcess(name)
     // Explicit stop clears intent so the monitor will not resurrect it.
     removeDesiredAgent(name)
@@ -1505,6 +1609,17 @@
     // Optional { "fresh": true } body -> no `--continue` (see /start note).
     let restartFresh = false
     try { restartFresh = JSON.parse((await readBody(req)).toString() || '{}').fresh === true } catch {}
+    if (readAgentProvider(name) === 'codex') {
+      try {
+        const status = await restartCodexAgent(name, restartFresh)
+        addDesiredAgent(name)
+        json(res, { ok: true, provider: 'codex', status })
+      } catch (err) {
+        logger.error({ err, name }, 'Codex agent restart failed')
+        json(res, { error: err instanceof Error ? err.message : 'Codex Bridge restart failed' }, 502)
+      }
+      return true
+    }
     const result = restartAgentProcess(name, { fresh: restartFresh })
     if (result.ok) { json(res, { ok: true }); return true }
     json(res, { error: result.error }, 400)
@@ -1515,6 +1630,10 @@
   if (statusMatch && method === 'GET') {
     const name = decodeURIComponent(statusMatch[1])
     if (!existsSync(agentDir(name))) { json(res, { error: 'Agent not found' }, 404); return true }
+    if (readAgentProvider(name) === 'codex') {
+      json(res, await getCodexAgentStatus(name))
+      return true
+    }
     json(res, getAgentProcessInfo(name))
     return true
   }
@@ -1535,6 +1654,32 @@
     const data = JSON.parse(body.toString()) as {
       claudeMd?: string; soulMd?: string; mcpJson?: string; model?: string
       authMode?: AuthMode; apiKey?: string; memoryIsolation?: boolean
+      provider?: AgentProvider; providerConfig?: Partial<CodexProviderConfig>
+    }
+    if (data.provider !== undefined && data.provider !== 'claude' && data.provider !== 'codex') {
+      json(res, { error: 'provider must be claude or codex' }, 400)
+      return true
+    }
+    const previousProvider = readAgentProvider(name)
+    if (data.provider !== undefined && data.provider !== previousProvider) {
+      // Stop the old runtime before persisting the provider switch. Otherwise
+      // a Claude tmux process and a Codex thread could remain live under one id.
+      if (previousProvider === 'codex') {
+        try {
+          await stopCodexAgent(name)
+        } catch (err) {
+          logger.error({ err, name }, 'Old Codex provider could not be stopped during provider switch')
+          json(res, { error: err instanceof Error ? err.message : 'Old Codex provider could not be stopped' }, 409)
+          return true
+        }
+      } else {
+        const stopped = stopAgentProcess(name)
+        if (!stopped.ok && !/not running|not found/i.test(stopped.error ?? '')) {
+          json(res, { error: stopped.error || 'Old Claude provider could not be stopped' }, 409)
+          return true
+        }
+      }
+      removeDesiredAgent(name)
     }
     if (data.memoryIsolation !== undefined) {
       // The main agent's cwd IS the install repo root, which is already a git
@@ -1550,6 +1695,17 @@
     if (data.soulMd !== undefined) atomicWriteFileSync(join(agentDir(name), 'SOUL.md'), data.soulMd)
     if (data.mcpJson !== undefined) atomicWriteFileSync(join(agentDir(name), '.mcp.json'), data.mcpJson)
     if (data.model !== undefined) writeAgentModel(name, data.model)
+    if (data.provider !== undefined) {
+      writeAgentProvider(name, data.provider)
+    }
+    if (data.providerConfig !== undefined) {
+      writeAgentCodexProviderConfig(name, {
+        ...data.providerConfig,
+        model: data.model ?? data.providerConfig.model,
+      })
+    } else if (data.model !== undefined && readAgentProvider(name) === 'codex') {
+      writeAgentCodexProviderConfig(name, { model: data.model })
+    }
     if (data.authMode !== undefined) {
       writeAgentAuthMode(name, data.authMode)
       if (data.authMode === 'api' && typeof data.apiKey === 'string' && data.apiKey.trim()) {
@@ -1567,7 +1723,20 @@
     const name = decodeURIComponent(agentMatch[1])
     const dir = agentDir(name)
     if (!existsSync(dir)) { json(res, { error: 'Agent not found' }, 404); return true }
+    if (readAgentProvider(name) === 'codex') {
+      try {
+        await codexBridgeClient.request('DELETE', `/v1/agents/${encodeURIComponent(name)}`)
+      } catch (err) {
+        json(res, {
+          error: `Codex Bridge cleanup failed; the Béla agent was not deleted: ${
+            err instanceof Error ? err.message : String(err)
+          }`,
+        }, 502)
+        return true
+      }
+    }
     rmSync(dir, { recursive: true, force: true })
+    removeDesiredAgent(name)
     cleanupTeamReferences(name)
     json(res, { ok: true })
     return true
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/routes/codex-approvals.ts marveen-1.21.1-0.1.7/src/web/routes/codex-approvals.ts
--- marveen-1.21.1-current/src/web/routes/codex-approvals.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/routes/codex-approvals.ts	2026-07-28 15:40:20.306548195 +0900
@@ -0,0 +1,63 @@
+import { codexBridgeClient } from '../../providers/codex-bridge-client.js'
+import { json, readBody } from '../http-helpers.js'
+import type { RouteContext } from './types.js'
+
+interface BridgeApproval {
+  approvalId: string
+  agentId: string
+  category: string
+  request: Record<string, unknown>
+  state: string
+  expiresAt: string
+  createdAt: string
+}
+
+export async function tryHandleCodexApprovals(ctx: RouteContext): Promise<boolean> {
+  const { req, res, path, method } = ctx
+
+  if (path === '/api/codex-approvals' && method === 'GET') {
+    try {
+      const result = await codexBridgeClient.request<BridgeApproval[]>('GET', '/v1/approvals')
+      json(res, result.data.map(approval => ({
+        id: approval.approvalId,
+        agentId: approval.agentId,
+        category: approval.category,
+        request: approval.request,
+        state: approval.state,
+        expiresAt: approval.expiresAt,
+        createdAt: approval.createdAt,
+      })))
+    } catch (err) {
+      json(res, { error: err instanceof Error ? err.message : 'Codex approvals unavailable' }, 502)
+    }
+    return true
+  }
+
+  const match = path.match(/^\/api\/codex-approvals\/([^/]+)$/)
+  if (match && method === 'PATCH') {
+    let body: { decision?: unknown }
+    try {
+      body = JSON.parse((await readBody(req, { maxBytes: 16 * 1024 })).toString())
+    } catch {
+      json(res, { error: 'Invalid JSON' }, 400)
+      return true
+    }
+    if (body.decision !== 'approve' && body.decision !== 'decline') {
+      json(res, { error: 'decision must be approve or decline' }, 400)
+      return true
+    }
+    try {
+      const result = await codexBridgeClient.request<Record<string, unknown>>(
+        'POST',
+        `/v1/approvals/${encodeURIComponent(match[1]!)}/decision`,
+        { decision: body.decision },
+      )
+      json(res, result.data)
+    } catch (err) {
+      json(res, { error: err instanceof Error ? err.message : 'Codex approval decision failed' }, 502)
+    }
+    return true
+  }
+
+  return false
+}
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/routes/codex-facade.ts marveen-1.21.1-0.1.7/src/web/routes/codex-facade.ts
--- marveen-1.21.1-current/src/web/routes/codex-facade.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/routes/codex-facade.ts	2026-07-28 15:40:20.306548195 +0900
@@ -0,0 +1,160 @@
+import { createHmac, timingSafeEqual } from 'node:crypto'
+import { readFileSync } from 'node:fs'
+import { homedir } from 'node:os'
+import { join } from 'node:path'
+import { MAIN_AGENT_ID } from '../../config.js'
+import {
+  createAgentMessage,
+  getAgentMessage,
+  getDb,
+  searchAgentMemories,
+  type Memory,
+} from '../../db.js'
+import { isKnownAgent, readAgentProvider } from '../agent-config.js'
+import { json, readBody } from '../http-helpers.js'
+import type { RouteContext } from './types.js'
+
+const TOKEN_FILE = process.env.BELA_CODEX_BRIDGE_TOKEN_FILE
+  || join(homedir(), '.config', 'bela-codex-bridge', 'token')
+const AGENT_ID = /^[a-z][a-z0-9-]{1,62}$/
+
+function authenticate(header: string | undefined): { agentId: string; scopedToken: string } | null {
+  const match = /^Bearer\s+(bcm1\.([a-z][a-z0-9-]{1,62})\.([A-Za-z0-9_-]+))$/.exec(header ?? '')
+  if (!match) return null
+  let master: string
+  try { master = readFileSync(TOKEN_FILE, 'utf8').trim() } catch { return null }
+  if (master.length < 32) return null
+  const expected = createHmac('sha256', master)
+    .update(`bela-codex-mcp:v1:${match[2]}`)
+    .digest('base64url')
+  const actualBuffer = Buffer.from(match[3]!)
+  const expectedBuffer = Buffer.from(expected)
+  if (actualBuffer.length !== expectedBuffer.length || !timingSafeEqual(actualBuffer, expectedBuffer)) return null
+  const agentId = match[2]!
+  if (!AGENT_ID.test(agentId) || !isKnownAgent(agentId) || readAgentProvider(agentId) !== 'codex') return null
+  return { agentId, scopedToken: match[1]! }
+}
+
+function memoryOpaqueId(scopedToken: string, id: number): string {
+  const mac = createHmac('sha256', scopedToken).update(`memory:v1:${id}`).digest('base64url').slice(0, 24)
+  return `${id}.${mac}`
+}
+
+function memoryView(memory: Memory, scopedToken: string): Record<string, unknown> {
+  return {
+    id: memoryOpaqueId(scopedToken, memory.id),
+    content: memory.content.slice(0, 20_000),
+    sector: memory.sector,
+    category: memory.category,
+    agentId: memory.agent_id,
+    keywords: memory.keywords,
+    createdAt: memory.created_at,
+    accessedAt: memory.accessed_at,
+  }
+}
+
+function resolveMemory(
+  opaque: string,
+  agentId: string,
+  scopedToken: string,
+): Memory | null {
+  const match = /^([1-9]\d*)\.([A-Za-z0-9_-]{24})$/.exec(opaque)
+  if (!match) return null
+  const id = Number(match[1])
+  const expected = memoryOpaqueId(scopedToken, id)
+  const actualBuffer = Buffer.from(opaque)
+  const expectedBuffer = Buffer.from(expected)
+  if (actualBuffer.length !== expectedBuffer.length || !timingSafeEqual(actualBuffer, expectedBuffer)) return null
+  return getDb().prepare(
+    "SELECT * FROM memories WHERE id = ? AND (agent_id = ? OR category = 'shared')",
+  ).get(id, agentId) as Memory | undefined ?? null
+}
+
+export async function tryHandleCodexFacade(ctx: RouteContext): Promise<boolean> {
+  const { req, res, path, method } = ctx
+  if (!path.startsWith('/api/codex-facade/')) return false
+
+  const identity = authenticate(req.headers.authorization)
+  if (!identity) {
+    json(res, { error: 'Unauthorized Codex facade token' }, 401)
+    return true
+  }
+
+  if (path === '/api/codex-facade/messages' && method === 'POST') {
+    let body: Record<string, unknown>
+    try {
+      body = JSON.parse((await readBody(req, { maxBytes: 300 * 1024 })).toString())
+    } catch {
+      json(res, { error: 'Invalid JSON' }, 400)
+      return true
+    }
+    const rawTo = typeof body.to === 'string' ? body.to.trim() : ''
+    const to = rawTo === 'bela' ? MAIN_AGENT_ID : rawTo
+    const content = typeof body.content === 'string' ? body.content.trim() : ''
+    const validTarget = to === MAIN_AGENT_ID || isKnownAgent(to)
+    if (!validTarget || !content || Buffer.byteLength(content) > 256 * 1024) {
+      json(res, { error: 'Invalid target or content' }, 400)
+      return true
+    }
+    const message = createAgentMessage(identity.agentId, to, content)
+    json(res, { ok: true, message: { id: message.id, status: message.status, to: message.to_agent } }, 201)
+    return true
+  }
+
+  const messageMatch = path.match(/^\/api\/codex-facade\/messages\/([1-9]\d*)$/)
+  if (messageMatch && method === 'GET') {
+    const message = getAgentMessage(Number(messageMatch[1]))
+    if (!message || message.from_agent !== identity.agentId) {
+      json(res, { error: 'Message not found' }, 404)
+      return true
+    }
+    json(res, {
+      ok: true,
+      message: {
+        id: message.id,
+        to: message.to_agent,
+        status: message.status,
+        result: message.result,
+        createdAt: message.created_at,
+        completedAt: message.completed_at,
+      },
+    })
+    return true
+  }
+
+  if (path === '/api/codex-facade/memories/search' && method === 'POST') {
+    let body: Record<string, unknown>
+    try {
+      body = JSON.parse((await readBody(req, { maxBytes: 16 * 1024 })).toString())
+    } catch {
+      json(res, { error: 'Invalid JSON' }, 400)
+      return true
+    }
+    const query = typeof body.query === 'string' ? body.query.trim().slice(0, 1000) : ''
+    const limit = Number.isInteger(body.limit) ? Math.min(20, Math.max(1, Number(body.limit))) : 10
+    if (!query) {
+      json(res, { error: 'query is required' }, 400)
+      return true
+    }
+    const memories = searchAgentMemories(identity.agentId, query, limit)
+    json(res, {
+      ok: true,
+      memories: memories.map((memory) => memoryView(memory, identity.scopedToken)),
+    })
+    return true
+  }
+
+  const memoryMatch = path.match(/^\/api\/codex-facade\/memories\/([^/]+)$/)
+  if (memoryMatch && method === 'GET') {
+    const memory = resolveMemory(decodeURIComponent(memoryMatch[1]!), identity.agentId, identity.scopedToken)
+    if (!memory) {
+      json(res, { error: 'Memory not found' }, 404)
+      return true
+    }
+    json(res, { ok: true, memory: memoryView(memory, identity.scopedToken) })
+    return true
+  }
+
+  json(res, { error: 'Codex facade route not found' }, 404)
+  return true
+}
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/routes/provider-callbacks.ts marveen-1.21.1-0.1.7/src/web/routes/provider-callbacks.ts
--- marveen-1.21.1-current/src/web/routes/provider-callbacks.ts	1970-01-01 09:00:00.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/routes/provider-callbacks.ts	2026-07-28 22:31:59.591332074 +0900
@@ -0,0 +1,160 @@
+import { timingSafeEqual } from 'node:crypto'
+import { readFileSync } from 'node:fs'
+import { homedir } from 'node:os'
+import { join } from 'node:path'
+import { MAIN_AGENT_ID } from '../../config.js'
+import {
+  createAgentMessage,
+  getAgentMessage,
+  getDb,
+  markMessageDone,
+  markMessageFailed,
+} from '../../db.js'
+import { logger } from '../../logger.js'
+import { readAgentProvider } from '../agent-config.js'
+import { json, readBody } from '../http-helpers.js'
+import type { RouteContext } from './types.js'
+
+const TOKEN_FILE = process.env.BELA_CODEX_BRIDGE_TOKEN_FILE
+  || join(homedir(), '.config', 'bela-codex-bridge', 'token')
+
+function bridgeAuthorized(header: string | undefined): boolean {
+  const match = /^Bearer\s+(.+)$/.exec(header ?? '')
+  if (!match) return false
+  let expected: string
+  try { expected = readFileSync(TOKEN_FILE, 'utf8').trim() } catch { return false }
+  const actualBuffer = Buffer.from(match[1]!.trim())
+  const expectedBuffer = Buffer.from(expected)
+  return expectedBuffer.length >= 32
+    && actualBuffer.length === expectedBuffer.length
+    && timingSafeEqual(actualBuffer, expectedBuffer)
+}
+
+interface CallbackRun {
+  runId?: unknown
+  agentId?: unknown
+  state?: unknown
+  startedAt?: unknown
+  finalResponse?: unknown
+  errorCode?: unknown
+  errorMessage?: unknown
+  context?: unknown
+}
+
+export const CODEX_CALLBACK_ADAPTER_REVISION = 2
+
+export function hasEquivalentAgentReply(
+  fromAgent: string,
+  toAgent: string,
+  content: string,
+  notBefore: number,
+): boolean {
+  const row = getDb().prepare(`
+    SELECT id
+    FROM agent_messages
+    WHERE from_agent = ?
+      AND to_agent = ?
+      AND content = ?
+      AND created_at >= ?
+    ORDER BY id DESC
+    LIMIT 1
+  `).get(fromAgent, toAgent, content, notBefore)
+  return Boolean(row)
+}
+
+export async function tryHandleProviderCallbacks(ctx: RouteContext): Promise<boolean> {
+  const { req, res, path, method } = ctx
+  if (path !== '/api/provider-callbacks/codex' || method !== 'POST') return false
+
+  if (!bridgeAuthorized(req.headers.authorization)) {
+    json(res, { error: 'Unauthorized Bridge callback' }, 401)
+    return true
+  }
+
+  let payload: { schemaVersion?: unknown; run?: CallbackRun }
+  try {
+    payload = JSON.parse((await readBody(req, { maxBytes: 2 * 1024 * 1024 })).toString())
+  } catch {
+    json(res, { error: 'Invalid callback JSON' }, 400)
+    return true
+  }
+  const run = payload.run
+  const context = run?.context && typeof run.context === 'object'
+    ? run.context as Record<string, unknown>
+    : {}
+  const messageId = Number(context.belaMessageId)
+  if (
+    payload.schemaVersion !== 1
+    || !run
+    || typeof run.runId !== 'string'
+    || typeof run.agentId !== 'string'
+    || typeof run.state !== 'string'
+    || !Number.isSafeInteger(messageId)
+    || messageId <= 0
+  ) {
+    json(res, { error: 'Invalid callback schema' }, 400)
+    return true
+  }
+
+  const db = getDb()
+  const result = db.transaction(() => {
+    const original = getAgentMessage(messageId)
+    if (!original) return { status: 404, body: { error: 'Original Béla message not found' } }
+    if (original.to_agent !== run.agentId || readAgentProvider(run.agentId) !== 'codex') {
+      return { status: 409, body: { error: 'Callback agent/message mismatch' } }
+    }
+    if (original.status === 'done' || original.status === 'failed') {
+      return { status: 200, body: { ok: true, duplicate: true } }
+    }
+
+    if (run.state === 'succeeded') {
+      const reply = typeof run.finalResponse === 'string' ? run.finalResponse.trim() : ''
+      const target = original.from_agent === 'system' ? MAIN_AGENT_ID : original.from_agent
+      const startedAt = typeof run.startedAt === 'string'
+        ? Math.floor(Date.parse(run.startedAt) / 1000)
+        : Number.NaN
+      const notBefore = Number.isSafeInteger(startedAt)
+        ? startedAt
+        : (original.delivered_at ?? original.created_at)
+      const duplicateReply = Boolean(reply) && hasEquivalentAgentReply(
+        run.agentId,
+        target,
+        reply,
+        notBefore,
+      )
+      if (reply && !duplicateReply) {
+        createAgentMessage(run.agentId, target, reply)
+      }
+      markMessageDone(messageId, `Codex run ${run.runId} completed`)
+      return {
+        status: 200,
+        body: {
+          ok: true,
+          replyQueued: Boolean(reply) && !duplicateReply,
+          replyDuplicate: duplicateReply,
+        },
+      }
+    }
+
+    const reason = [
+      typeof run.errorCode === 'string' ? run.errorCode : 'codex_run_failed',
+      typeof run.errorMessage === 'string' ? run.errorMessage : `state=${run.state}`,
+    ].join(': ')
+    markMessageFailed(messageId, reason.slice(0, 2000))
+    createAgentMessage(
+      'system',
+      MAIN_AGENT_ID,
+      `[codex-bridge-failure] ${run.agentId} nem tudta feldolgozni a(z) #${messageId} üzenetet: ${reason.slice(0, 500)}`,
+    )
+    return { status: 200, body: { ok: true, failed: true } }
+  })()
+
+  logger.info({
+    runId: run.runId,
+    agentId: run.agentId,
+    messageId,
+    state: run.state,
+  }, 'Codex Bridge callback processed')
+  json(res, result.body, result.status)
+  return true
+}
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web/schedule-runner.ts marveen-1.21.1-0.1.7/src/web/schedule-runner.ts
--- marveen-1.21.1-current/src/web/schedule-runner.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web/schedule-runner.ts	2026-07-28 15:40:20.306548195 +0900
@@ -16,6 +16,7 @@
   insertPendingTaskRetryIfNew,
   markPendingTaskRetryAlert,
   clearPendingTaskRetryAlert,
+  createAgentMessage,
 } from '../db.js'
 import { toPendingRetryView, classifyTelegramSendError, type PendingRetryView } from '../pending-retries.js'
 import {
@@ -27,7 +28,7 @@
   listScheduledTasks,
   type ScheduledTask,
 } from './scheduled-tasks-io.js'
-import { listAgentNames, readFileOr, readAgentRemoteHost } from './agent-config.js'
+import { listAgentNames, readFileOr, readAgentProvider, readAgentRemoteHost } from './agent-config.js'
 import {
   agentSessionName,
   isAgentRunning,
@@ -121,6 +122,18 @@
 // pendingTaskRetries loop and the normal cron loop share one code path.
 function attemptFireTask(task: ScheduledTask, agentName: string, now: number): 'fired' | 'busy' | 'missing' | 'starting' | 'error' {
   const isMainAgent = agentName === MAIN_AGENT_ID
+  if (!isMainAgent && readAgentProvider(agentName) === 'codex') {
+    const fullPrompt =
+      SCHEDULED_TASK_PREAMBLE + '\n' +
+      `[Utemezett feladat: ${task.name}]\n\n` +
+      wrapScheduledTask(`scheduled-task:${task.name}`, task.prompt)
+    createAgentMessage('system', agentName, fullPrompt)
+    scheduleLastRun.set(task.name, now)
+    persistScheduleLastRun()
+    appendTaskRun(task.name, agentName, 'fired')
+    logger.info({ task: task.name, agent: agentName }, 'Scheduled task queued for Codex Bridge')
+    return 'fired'
+  }
   // Allow per-task session override via targetSession config field.
   // Falls back to the standard agent session name derivation.
   const session = task.targetSession
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/src/web.ts marveen-1.21.1-0.1.7/src/web.ts
--- marveen-1.21.1-current/src/web.ts	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/src/web.ts	2026-07-28 15:40:20.306548195 +0900
@@ -4,6 +4,7 @@
 import { execSync, execFileSync } from 'node:child_process'
 import { PROJECT_ROOT, WEB_HOST, DASHBOARD_PUBLIC_URL, DASHBOARD_ALLOWED_ORIGINS, MAIN_AGENT_ID } from './config.js'
 import { loadOrCreateDashboardToken, checkBearerToken } from './web/dashboard-auth.js'
+import { isProviderServiceEndpoint } from './web/provider-service-auth.js'
 import { isBlockedCrossOriginWrite, originMatchesServedHost } from './web/csrf-origin.js'
 import { json } from './web/http-helpers.js'
 import { detectLanIp } from './web/network-info.js'
@@ -27,6 +28,9 @@
 import { tryHandleMessages } from './web/routes/messages.js'
 import { tryHandleAgentTerminal } from './web/routes/agent-terminal.js'
 import { tryHandleAgentConversation } from './web/routes/agent-conversation.js'
+import { tryHandleProviderCallbacks } from './web/routes/provider-callbacks.js'
+import { tryHandleCodexFacade } from './web/routes/codex-facade.js'
+import { tryHandleCodexApprovals } from './web/routes/codex-approvals.js'
 import { tryHandleAgentTaskState } from './web/routes/agent-taskstate.js'
 import { sweepOrphanTaskStates } from './web/agent-taskstate.js'
 import { tryHandleDailyLog } from './web/routes/daily-log.js'
@@ -119,6 +123,7 @@
     // via <img src> which can't carry headers -- these are non-sensitive assets).
     const isPublicApi =
       (path === '/api/auth/status' && method === 'GET') ||
+      isProviderServiceEndpoint(path, method) ||
       (method === 'GET' && (
         path === '/api/marveen/avatar' ||
         /^\/api\/agents\/[^/]+\/avatar$/.test(path)
@@ -168,6 +173,9 @@
       if (await tryHandleSkills(routeCtx)) return
       if (await tryHandleAgentTerminal(routeCtx)) return
       if (await tryHandleAgentConversation(routeCtx)) return
+      if (await tryHandleProviderCallbacks(routeCtx)) return
+      if (await tryHandleCodexFacade(routeCtx)) return
+      if (await tryHandleCodexApprovals(routeCtx)) return
       if (await tryHandleAgentTaskState(routeCtx)) return
       if (await tryHandleAgents(routeCtx, WEB_DIR)) return
       if (await tryHandleMarveen(routeCtx, WEB_DIR)) return
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/web/app.js marveen-1.21.1-0.1.7/web/app.js
--- marveen-1.21.1-current/web/app.js	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/web/app.js	2026-07-28 15:40:20.310548195 +0900
@@ -2040,6 +2040,7 @@
 const agentName = document.getElementById('agentName')
 const agentDesc = document.getElementById('agentDesc')
 const agentModel = document.getElementById('agentModel')
+const agentProvider = document.getElementById('agentProvider')
 const toast = document.getElementById('toast')
 
 const AVATARS = [
@@ -2162,6 +2163,7 @@
   agentName.value = ''
   agentDesc.value = ''
   agentModel.value = 'inherit'
+  agentProvider.value = 'claude'
   loadAvailableModels()
   selectedAvatar = null
   selectedAvatarFile = null
@@ -2177,9 +2179,30 @@
     document.getElementById('agentProfileDesc'),
     'default',
   )
+  updateWizardProviderUI()
   updateWizardUI()
 }
 
+agentProvider.addEventListener('change', () => {
+  if (agentProvider.value === 'codex') agentModel.value = 'gpt-5.6-terra'
+  else if (agentModel.value.startsWith('gpt-')) agentModel.value = 'inherit'
+  updateWizardProviderUI()
+})
+
+function updateWizardProviderUI() {
+  const codex = agentProvider.value === 'codex'
+  const identityLabel = document.getElementById('wizardClaudeMdLabel')
+  const soulLabel = document.getElementById('wizardSoulMdLabel')
+  const hint = document.getElementById('wizardGenHint')
+  if (identityLabel) identityLabel.textContent = codex
+    ? 'Agent instrukciók (Codex AGENTS.md forrás)'
+    : 'CLAUDE.md'
+  if (soulLabel) soulLabel.textContent = codex ? 'Agent személyiség' : 'SOUL.md'
+  if (hint) hint.textContent = codex
+    ? 'A Codex-kompatibilis instrukciók helyben, Claude-függőség nélkül készülnek'
+    : t('agents.wizard.gen_hint')
+}
+
 function updateWizardUI() {
   // Steps indicator
   document.querySelectorAll('#wizardSteps .wizard-step').forEach((s) => {
@@ -2197,6 +2220,7 @@
 document.getElementById('wizardNextBtn').addEventListener('click', async () => {
   const name = agentName.value.trim()
   const desc = agentDesc.value.trim()
+  const provider = agentProvider.value
   if (!name) { agentName.focus(); return }
   if (!desc) { agentDesc.focus(); return }
 
@@ -2204,7 +2228,9 @@
   updateWizardUI()
 
   const statusEl = document.getElementById('wizardGenStatus')
-  statusEl.textContent = t('agents.claude_md_generating')
+  statusEl.textContent = provider === 'codex'
+    ? 'Codex agent-instrukciók létrehozása...'
+    : t('agents.claude_md_generating')
 
   try {
     // Create agent via API (returns generated content)
@@ -2215,6 +2241,15 @@
         name,
         description: desc,
         model: agentModel.value,
+        provider,
+        providerConfig: provider === 'codex'
+          ? {
+              model: agentModel.value,
+              sandboxMode: 'workspace-write',
+              approvalPolicy: 'bela',
+              networkEnabled: false,
+            }
+          : undefined,
         profile: document.getElementById('agentProfile').value,
       }),
     })
@@ -2230,7 +2265,9 @@
     // like "étrendíró" still resolves to the real agent dir "etrendiro".
     const createdName = result.name || name
     wizardCreatedName = createdName
-    statusEl.textContent = t('agents.soul_md_generating')
+    statusEl.textContent = provider === 'codex'
+      ? 'Codex agent konfigurációjának ellenőrzése...'
+      : t('agents.soul_md_generating')
 
     // Fetch full agent details to get generated content
     const detailRes = await fetch(`/api/agents/${encodeURIComponent(createdName)}`)
@@ -2350,11 +2387,76 @@
       }
     }
     renderAgents()
+    void loadCodexApprovals()
   } catch (err) {
     console.error('Betöltés hiba:', err)
   }
 }
 
+document.getElementById('refreshCodexApprovalsBtn')?.addEventListener('click', () => {
+  void loadCodexApprovals()
+})
+
+async function loadCodexApprovals() {
+  const panel = document.getElementById('codexApprovalsPanel')
+  const list = document.getElementById('codexApprovalsList')
+  if (!panel || !list) return
+  const hasCodexAgent = Array.isArray(agents) && agents.some(agent => agent.provider === 'codex')
+  panel.style.display = hasCodexAgent ? '' : 'none'
+  if (!hasCodexAgent) return
+
+  list.innerHTML = '<p style="color:var(--text-muted)">Jóváhagyások betöltése…</p>'
+  try {
+    const response = await fetch('/api/codex-approvals')
+    const payload = await response.json()
+    if (!response.ok) throw new Error(payload.error || `HTTP ${response.status}`)
+    const pending = payload.filter(item => item.state === 'pending')
+    if (pending.length === 0) {
+      list.innerHTML = '<p style="color:var(--text-muted)">Nincs függőben lévő Codex-jóváhagyás.</p>'
+      return
+    }
+    list.innerHTML = pending.map(item => {
+      const request = escapeHtml(JSON.stringify(item.request, null, 2))
+      return `<div class="agent-card" style="display:block;margin-bottom:10px;padding:14px">
+        <div style="display:flex;justify-content:space-between;gap:12px;align-items:flex-start">
+          <div>
+            <strong>${escapeHtml(item.agentId)}</strong>
+            <span class="meta-badge" style="margin-left:6px">${escapeHtml(item.category)}</span>
+            <pre style="white-space:pre-wrap;overflow-wrap:anywhere;margin:10px 0 0;font-size:12px;color:var(--text-muted)">${request}</pre>
+          </div>
+          <div style="display:flex;gap:6px;flex-shrink:0">
+            <button class="btn-primary btn-compact codex-approval-decision" data-id="${escapeAttr(item.id)}" data-decision="approve">Jóváhagyás</button>
+            <button class="btn-danger btn-compact codex-approval-decision" data-id="${escapeAttr(item.id)}" data-decision="decline">Elutasítás</button>
+          </div>
+        </div>
+      </div>`
+    }).join('')
+    list.querySelectorAll('.codex-approval-decision').forEach(button => {
+      button.addEventListener('click', () => {
+        void decideCodexApproval(button.dataset.id, button.dataset.decision)
+      })
+    })
+  } catch (err) {
+    list.innerHTML = `<p style="color:var(--danger)">A Codex-jóváhagyások nem érhetők el: ${escapeHtml(err.message || String(err))}</p>`
+  }
+}
+
+async function decideCodexApproval(id, decision) {
+  try {
+    const response = await fetch(`/api/codex-approvals/${encodeURIComponent(id)}`, {
+      method: 'PATCH',
+      headers: { 'Content-Type': 'application/json' },
+      body: JSON.stringify({ decision }),
+    })
+    const payload = await response.json()
+    if (!response.ok) throw new Error(payload.error || `HTTP ${response.status}`)
+    showToast(decision === 'approve' ? 'Codex művelet jóváhagyva.' : 'Codex művelet elutasítva.')
+    await loadCodexApprovals()
+  } catch (err) {
+    showToast(`Codex jóváhagyási hiba: ${err.message || String(err)}`)
+  }
+}
+
 // Format a context-token count for display (e.g. 699884 -> "≈700k token").
 function formatContextTokens(n) {
   if (typeof n !== 'number' || !isFinite(n) || n <= 0) return '-'
@@ -2716,6 +2818,13 @@
 
   // Settings tab - load Ollama + DeepSeek models then set value
   loadAvailableModels()
+  const providerSelect = document.getElementById('editAgentProvider')
+  providerSelect.value = currentAgent.provider || 'claude'
+  providerSelect.onchange = () => {
+    const modelSelect = document.getElementById('editAgentModel')
+    if (providerSelect.value === 'codex') modelSelect.value = 'gpt-5.6-terra'
+    else if (modelSelect.value.startsWith('gpt-')) modelSelect.value = 'claude-opus-4-8[1m]'
+  }
   loadOllamaModels().then(() => {
     document.getElementById('editAgentModel').value = currentAgent.activeModel || currentAgent.model || 'claude-opus-4-8[1m]'
   })
@@ -3233,15 +3342,28 @@
 document.getElementById('saveModelBtn').addEventListener('click', async () => {
   if (!currentAgent || currentAgent.role === 'main') return
   const newModel = document.getElementById('editAgentModel').value
+  const newProvider = document.getElementById('editAgentProvider').value
   const name = currentAgent.name
   try {
     const res = await fetch(`/api/agents/${encodeURIComponent(name)}`, {
       method: 'PUT',
       headers: { 'Content-Type': 'application/json' },
-      body: JSON.stringify({ model: newModel }),
+      body: JSON.stringify({
+        model: newModel,
+        provider: newProvider,
+        providerConfig: newProvider === 'codex'
+          ? {
+              model: newModel,
+              sandboxMode: currentAgent.providerConfig?.sandboxMode || 'workspace-write',
+              approvalPolicy: currentAgent.providerConfig?.approvalPolicy || 'bela',
+              networkEnabled: currentAgent.providerConfig?.networkEnabled === true,
+            }
+          : undefined,
+      }),
     })
     if (!res.ok) throw new Error()
     currentAgent.model = newModel
+    currentAgent.provider = newProvider
     const triggeredAt = Math.floor(Date.now() / 1000)
     document.getElementById('agentDetailModelRestarting').hidden = false
     document.getElementById('processLabel').textContent = t('agents.process_label')
@@ -3255,6 +3377,20 @@
       showToast(t('agents.restart_failed'))
       return
     }
+    if (newProvider === 'codex') {
+      const statusRes = await fetch(`/api/agents/${encodeURIComponent(name)}/status`)
+      const status = statusRes.ok ? await statusRes.json() : null
+      document.getElementById('agentDetailModelRestarting').hidden = true
+      document.getElementById('processDot').className = status?.actualState === 'idle'
+        || status?.actualState === 'busy'
+        ? 'process-dot running'
+        : 'process-dot'
+      document.getElementById('processLabel').textContent = status?.actualState || 'Codex Bridge'
+      document.getElementById('agentDetailModel').textContent = status?.model || newModel
+      showToast('Codex agent újraindítva')
+      loadAgents()
+      return
+    }
     startModelRestartPolling(name, newModel, triggeredAt)
   } catch { showToast(t('common.error_save')) }
 })
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=store' '--exclude=agents' marveen-1.21.1-current/web/index.html marveen-1.21.1-0.1.7/web/index.html
--- marveen-1.21.1-current/web/index.html	2026-07-11 21:56:44.000000000 +0900
+++ marveen-1.21.1-0.1.7/web/index.html	2026-07-28 15:40:20.310548195 +0900
@@ -347,6 +347,16 @@
           <span data-i18n="agents.page.new_btn">Új ügynök</span>
         </button>
       </div>
+      <section id="codexApprovalsPanel" style="display:none;margin-top:24px">
+        <div class="page-header-row" style="margin-bottom:10px">
+          <div>
+            <h2 style="font-size:18px;margin:0">Codex jóváhagyások</h2>
+            <p class="subtitle" style="margin-top:4px">Függőben lévő eszköz- és műveletkérések</p>
+          </div>
+          <button class="btn-secondary btn-compact" id="refreshCodexApprovalsBtn">Frissítés</button>
+        </div>
+        <div id="codexApprovalsList"></div>
+      </section>
     </div>
 
     <!-- === Activity (live agent status) Page === -->
@@ -1888,8 +1898,21 @@
             <textarea id="agentDesc" rows="4" placeholder="pl. Piackutatást végez, versenytársakat elemez, táblázatos összefoglalókat készít magyarul." data-i18n-placeholder="agents.create.desc_ph"></textarea>
           </div>
           <div class="form-group">
+            <label for="agentProvider">Agent motor</label>
+            <select id="agentProvider">
+              <option value="claude">Claude Code</option>
+              <option value="codex">OpenAI Codex Bridge (ChatGPT előfizetés)</option>
+            </select>
+            <small style="display:block;margin-top:6px;color:var(--text-muted);font-size:12px">
+              A Codex agent külön Bridge szolgáltatásban fut, Béla továbbra is a főagent.
+            </small>
+          </div>
+          <div class="form-group">
             <label for="agentModel" data-i18n="agents.detail.model_label">Modell</label>
             <select id="agentModel">
+              <optgroup label="🤖 OpenAI Codex">
+                <option value="gpt-5.6-terra">GPT-5.6 Terra (ChatGPT előfizetés)</option>
+              </optgroup>
               <option value="inherit" data-i18n="agents.model.inherit">Öröklött (alapértelmezett)</option>
               <optgroup label="☁️ Claude (felhő)">
                 <option value="claude-fable-5" data-i18n="agents.model.fable5">Fable 5 (legújabb)</option>
@@ -1918,18 +1941,18 @@
               <span class="spinner wizard-spin"></span>
             </div>
             <p class="wizard-gen-status" id="wizardGenStatus" data-i18n="agents.wizard.generating_status">CLAUDE.md generálás...</p>
-            <p class="wizard-gen-sub" data-i18n="agents.wizard.gen_hint">Ez néhány másodpercig tarthat</p>
+            <p class="wizard-gen-sub" id="wizardGenHint" data-i18n="agents.wizard.gen_hint">Ez néhány másodpercig tarthat</p>
           </div>
         </div>
 
         <!-- Step 3: Review -->
         <div class="wizard-panel" id="wizardStep3" hidden>
           <div class="form-group">
-            <label for="wizardClaudeMd">CLAUDE.md</label>
+            <label for="wizardClaudeMd" id="wizardClaudeMdLabel">CLAUDE.md</label>
             <textarea id="wizardClaudeMd" class="code-textarea" rows="10"></textarea>
           </div>
           <div class="form-group">
-            <label for="wizardSoulMd">SOUL.md</label>
+            <label for="wizardSoulMd" id="wizardSoulMdLabel">SOUL.md</label>
             <textarea id="wizardSoulMd" class="code-textarea" rows="8"></textarea>
           </div>
           <div class="wizard-actions">
@@ -2043,8 +2066,18 @@
             </div>
           </div>
           <div class="form-group">
+            <label for="editAgentProvider">Agent motor</label>
+            <select id="editAgentProvider">
+              <option value="claude">Claude Code</option>
+              <option value="codex">OpenAI Codex Bridge</option>
+            </select>
+          </div>
+          <div class="form-group">
             <label for="editAgentModel" data-i18n="agents.detail.model_label">Modell</label>
             <select id="editAgentModel">
+              <optgroup label="🤖 OpenAI Codex">
+                <option value="gpt-5.6-terra">GPT-5.6 Terra (ChatGPT előfizetés)</option>
+              </optgroup>
               <optgroup label="☁️ Claude (felhő)">
                 <option value="claude-fable-5" data-i18n="agents.model.fable5">Fable 5 (legújabb)</option>
                 <option value="claude-opus-4-8[1m]" data-i18n="agents.model.opus48">Opus 4.8 (1M kontextus, alapértelmezett)</option>
